interface Props{
    id: String,
    currentUserId: String,
    parentId: String,
    content: String,
    author: String,
    community: String,
    createdAt: Date,
    comments: String,
}


const ThreadCard = ({
    id,
    currentUserId,
    parentId,
    content,
    author,
    community,
    createdAt,
    comments,
}: Props) => {

}